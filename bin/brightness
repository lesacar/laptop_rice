#!/bin/bash

if [ "$1" = "up" ]; then
	xbacklight -inc 5
elif [ "$1" = "down" ]; then
	xbacklight -dec 5
elif [ "$1" = "ask" ]; then
	val=$(dmenu -p 'Set Brightness to: ' < /dev/null)
	xbacklight -set $val
fi

dunstctl close-all && notify-send $(xbacklight)

